prompt = "If you tell us who you are, we can personalize the messages you see."
prompt += "\nWhat is your first name? "

name = input(prompt)
print(f"\nHello, {name}!")

#  >>> age = input("How old are you? ")
#   How old are you? 21
# >>> age = int(age)
#   >>> age >= 18
#  True
# the above is for integers becasue if you dont use int() it is read as a string